>>> import sys
>>> sys.path.append("json")
>>> import json_parser
>>> d = json_parser.parse('{ "Object":{"Zoom": false, "Property1":{"Property2":{"Color":[0,153,255,0]},"Width":40}} }')
>>> d == {'Object': {'Zoom': False, 'Property1': {'Property2': {'Color': [0.0, 153.0, 255.0, 0.0]}, 'Width': 40.0}}}
True
>>> sys.path.append("halt")
>>> import halt
Program halts! 7 instructions executed.
>>> sys.path.append("pl0")
>>> import pl0
>>> with open("pl0/primes.pl0") as f: print(pl0.trans(f.read()))
class Mem:
    pass
M = Mem()
def read():
    return int(input())
def write(x):
    print(x)
M.max = 100
def isprime():
    M.ret = 1
    M.i = 2
    while (M.i < M.arg):
        if (((M.arg // M.i) * M.i) == M.arg):
            M.ret = 0
            M.i = M.arg
        M.i = (M.i + 1)
def primes():
    M.arg = 2
    while (M.arg < M.max):
        isprime()
        if (M.ret == 1):
            write(M.arg)
        M.arg = (M.arg + 1)
primes()
>>> sys.path.append("prop")
>>> from prop import prop_test
>>> prop_test()
('And', ('And', ('Atom', 'p'), ('Atom', 'q')), ('Atom', 'p'))
>>> sys.path.append("calc")
>>> from calc import calc
>>> calc("(1 + 2 + 3 + 4) - (10 / 2)")
('Int', 5)
